packages:
  - "apps/*"
  - "packages/*"
  - "services/*"

allowBuilds:
  esbuild: true
  sharp: true

# Dependencies allowed to run install/build scripts (pnpm blocks them by default).
onlyBuiltDependencies:
  - esbuild
  - sharp
